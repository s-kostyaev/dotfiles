#!/bin/bash

echo installing some tools for web development
sudo npm install -g eslint babel-eslint eslint-plugin-react
echo install tern for intelligent js
sudo npm install -g tern
echo install serve for see frontend in browser
sudo npm install -g serve
echo install webpack for building javascript with deps
sudo npm install -g webpack
echo install webpack-dev-server for autoreload changed files
sudo npm install -g webpack-dev-server
